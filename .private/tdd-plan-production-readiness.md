# TDD Plan — Production Readiness (3 Items)

## Items
1. Password panic → descriptive error + exit (TDD)
2. SIGTERM handler for graceful shutdown
3. docker-compose.yml + HEALTHCHECK

## Estimación: ~3h
